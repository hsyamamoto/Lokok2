# LOKOK2 - Exemplo de variáveis de produção
# Copie para .env.production e preencha com valores reais

# Ambiente e Servidor
NODE_ENV=production
PORT=3000
SESSION_SECRET=replace-with-strong-secret

# Fonte de dados (sem Excel local)

# Google Drive (opcional; usado em produção)
GOOGLE_DRIVE_FILE_ID=replace-with-file-id
GOOGLE_SERVICE_ACCOUNT_EMAIL=service-account@project.iam.gserviceaccount.com
# Para chaves privadas multilinha, inclua \n para quebras de linha ou use aspas.
GOOGLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...your-key...\n-----END PRIVATE KEY-----\n
# Países suportados (abas na planilha)
# Válidos: US, CA, MX, CN
DEFAULT_ADMIN_ALLOWED_COUNTRIES=US,CA,MX,CN

# Banco (opcional)
DATABASE_URL=postgresql://user:password@host:port/dbname?sslmode=require

# Diretório de dados (persistência de app; não usado para Excel)

# Exigir banco de dados para operações de escrita (desabilita fallback em writes)
# Defina como 1 ou true para forçar escrita apenas no DB
REQUIRE_DB=1

# Habilitar usuários no Postgres (login e gestão via DB)
USE_DB_USERS=1

# Metadados de deploy (preenchidos pelo Railway)
RAILWAY_GIT_COMMIT_SHA=
RAILWAY_GIT_BRANCH=
RAILWAY_GIT_REPO=
# --- Password fixes (aplicados em cada inicialização) ---
# Defina as senhas desejadas para usuários específicos.
# As senhas são atualizadas diretamente no banco de dados.
#FIX_PWD_IGNACIO=mudar123
#FIX_PWD_NACHO=mudar123
#FIX_PWD_JEISON=mudar123
#FIX_PWD_MARCELO=marcelo123

# Seed padrão de usuários (produção): desabilitado; usuários devem existir no DB
#ALLOW_DEFAULT_USERS_SEED=false

# Admin API token (para rotas administrativas como reset de senha via HTTP)
#ADMIN_MIGRATE_TOKEN=supersecreto-valor
